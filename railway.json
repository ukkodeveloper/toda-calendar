{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm run build:packages && pnpm --filter the-court-api prisma:generate && pnpm --filter the-court-api build"
  },
  "deploy": {
    "preDeployCommand": "pnpm --filter the-court-api exec prisma migrate deploy --schema prisma/schema.prisma",
    "startCommand": "pnpm --filter the-court-api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  }
}
